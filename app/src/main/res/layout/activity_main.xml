<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. 真正的 M3 SearchBar -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:liftOnScroll="true">

        <com.google.android.material.search.SearchBar
            android:id="@+id/searchBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/app_name"
            app:menu="@menu/menu_main" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. 点击后全屏展开的输入面板 -->
    <com.google.android.material.search.SearchView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="搜索工具、命令或应用"
        app:layout_anchor="@id/searchBar"
        app:menu="@menu/menu_main" />

    <!-- 3. 你原来的按钮区，一点不动 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="24dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- 大标题 -->
        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textAppearance="?attr/textAppearanceHeadlineLarge"
            android:textColor="?android:attr/textColorPrimary"
            android:visibility="gone" />

        <!-- 副标题 -->
        <TextView
            android:id="@+id/tv_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="80dp"
            android:text="@string/subtitle_not_connected"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/subtitle_gray" />

        <!-- 按钮区 -->
        <Button
            android:id="@+id/btn_apps_list"
            style="@style/Widget.AndroidToolBox.Home.Button"
            android:layout_marginTop="16dp"
            android:text="@string/btn_apps_list"/>

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/switch_screen"
            style="@style/Widget.AndroidToolBox.Home.Switch"
            android:text="@string/btn_screen_shortcut" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/switch_float"
            style="@style/Widget.AndroidToolBox.Home.Switch"
            android:text="@string/btn_global_float" />

        <Button
            android:id="@+id/btn_apk_manager"
            style="@style/Widget.AndroidToolBox.Home.Button"
            android:text="@string/btn_apk_manager" />

        <Button
            android:id="@+id/btn_more"
            style="@style/Widget.AndroidToolBox.Home.More"
            android:text="@string/btn_more" />

    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>